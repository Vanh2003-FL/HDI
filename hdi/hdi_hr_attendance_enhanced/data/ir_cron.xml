<?xml version="1.0" encoding="utf-8"?>
<odoo>
    
    <!-- Cron: Process Pending Attendance Logs -->
    <record id="cron_process_attendance_logs" model="ir.cron">
        <field name="name">Attendance: Process Pending Logs</field>
        <field name="model_id" ref="model_hr_attendance_log"/>
        <field name="state">code</field>
        <field name="code">model.cron_process_pending_logs()</field>
        <field name="interval_number">1</field>
        <field name="interval_type">minutes</field>
        <field name="numbercall">-1</field>
        <field name="active" eval="True"/>
        <field name="doall" eval="False"/>
    </record>
    
    <!-- Cron: Retry Failed Attendance Logs -->
    <record id="cron_retry_failed_logs" model="ir.cron">
        <field name="name">Attendance: Retry Failed Logs</field>
        <field name="model_id" ref="model_hr_attendance_log"/>
        <field name="state">code</field>
        <field name="code">model.cron_retry_failed_logs()</field>
        <field name="interval_number">5</field>
        <field name="interval_type">minutes</field>
        <field name="numbercall">-1</field>
        <field name="active" eval="True"/>
        <field name="doall" eval="False"/>
    </record>

</odoo>
