<?xml version="1.0" encoding="UTF-8"?>
<templates id="template" xml:space="preserve">
     <t t-extend="ListView.buttons">
        <t t-jquery="button.o_list_export_xlsx" t-operation="after">
            <div t-if="widget.modelName == 'ngsc.hr.performance.evaluation'">
                <button type="button" class="btn btn-primary o_list_update_performance_evaluation"><i id="sync_performance" class="fa fa-refresh"></i> Cập nhật</button>
            </div>
            <div t-if="widget.modelName == 'ngsc.hr.performance.evaluation'">
                <button type="button" class="btn btn-primary o_list_remind_performance_evaluation"><i id="remind_performance" class="fa fa-bell"></i> Nhắc nhở đánh giá</button>
            </div>
        </t>
    </t>
    <t t-extend="ListView.buttons">
        <t t-jquery="button.o_list_export_xlsx" t-operation="after">
            <div t-if="widget.modelName == 'task.evaluation'">
                <button type="button" class="btn btn-primary o_list_update_task_evaluation"><i id="sync_evaluation" class="fa fa-refresh"></i> Cập nhật</button>
            </div>
             <div t-if="widget.modelName == 'task.evaluation'">
                <button type="button" class="btn btn-primary o_list_remind_task_evaluation"><i id="remind_evaluation" class="fa fa-bell"></i> Nhắc nhở đánh giá</button>
            </div>
        </t>
    </t>
</templates>